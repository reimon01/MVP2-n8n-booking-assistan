# PostgreSQL (la base que usan los nodos Postgres de n8n)
DATABASE_URL=postgres://vida:vida@localhost:5432/vida_v2

# OpenAI / LLM (lo usa el nodo del LLM en n8n)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# WhatsApp Cloud API (lo usan los nodos de WhatsApp en n8n)
WHATSAPP_VERIFY_TOKEN=changeme
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
